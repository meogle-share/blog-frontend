# PR 생성

아래 컨벤션에 따라 현재 브랜치의 변경사항으로 PR을 생성한다.

## 절차

1. 현재 브랜치가 `main`이면, 아래 브랜치 네이밍 규칙에 따라 feature 브랜치를 생성하고 전환한다
2. `git log`와 `git diff main...HEAD`로 브랜치의 전체 변경사항 파악
3. 아래 컨벤션에 따라 PR 제목과 본문 작성
4. 원격에 push 후 `gh pr create`로 PR 생성

## PR 단위

- 하나의 PR = 하나의 목적 (리뷰어가 "이 PR은 왜 필요한가"에 한 문장으로 답할 수 있어야 함)
- 리팩터링과 기능 변경은 별도 PR로 분리 — 섞이면 리뷰 난이도가 급증한다
- 변경 규모가 크면 스택형 PR(base를 다른 feature 브랜치로)로 분할을 고려

## 제목 형식

`type(scope): 설명`

- 커밋 컨벤션의 type/scope 규칙을 동일하게 적용
- PR에 여러 커밋이 있으면 **가장 대표적인 변경**을 기준으로 type 결정
- **70자 이내** — GitHub PR 목록에서 잘리지 않는 한계

## 본문 구조

```markdown
## Why
이 변경이 필요한 배경 — 현재 상태의 문제점 또는 요구사항

## What
어떤 접근으로 해결했는지, 주요 설계 결정은 무엇인지

## Note (선택)
리뷰어가 알아야 할 부수 정보 — 하위 호환성, 후속 작업 등
```

### Why 작성 원칙

**리뷰어가 diff를 열기 전에 "왜 이 PR이 존재하는가"를 이해할 수 있어야 한다.**

- 현재 상태에서 어떤 문제가 있거나, 어떤 것이 부족한지 서술
- 코드가 아니라 상황을 설명 — "무엇이 불가능한지", "어떤 장애가 발생하는지"
- 이슈 트래커 링크가 있으면 첨부

### What 작성 원칙

**"어떻게 구현했는가"가 아니라 "어떤 결정을 내렸는가"에 집중한다.**

- 핵심 설계 결정과 그 이유를 자연스러운 문장으로 기술
- 대안이 있었다면 왜 선택하지 않았는지 간단히 언급
- 변경의 범위가 넓으면 페이지/컴포넌트 단위로 구조를 잡아 리뷰 동선을 안내
- 분량 제한 없음 — 변경의 복잡도에 맞게 필요한 만큼 작성

> **안티패턴**: "A를 추가했습니다. B를 수정했습니다." — diff 나열은 본문이 아니다.
> 코드를 보면 알 수 있는 것(what changed)이 아니라, 코드만으로는 알 수 없는 것(why this way)을 쓴다.

## 브랜치 네이밍

`type/간결한-설명`

```
feat/login-page
fix/markdown-rendering
refactor/remove-unused-components
chore/upgrade-next-15
```

## 리뷰 & 머지

- 리뷰어 지정 후 approve 없이 머지하지 않는다 (1인 프로젝트 기간에는 자기 리뷰 허용)
- CI(lint, build)가 모두 통과해야 머지 가능
- **Squash merge** 기본 — 커밋 히스토리를 깔끔하게 유지
- 머지 후 원격 브랜치는 삭제

## 예시

### 기능 추가

```markdown
# 제목
feat(auth): 로그인 페이지 추가

# 본문
## Why
블로그에 접근하려면 인증이 필요하지만 로그인 UI가 아직 없어
사용자가 로그인할 수 없는 상태다.

## What
`/login` 경로에 이메일/비밀번호 로그인 폼을 추가한다.
폼 상태 관리는 React Hook Form 대신 controlled component로 구현했는데,
필드가 2개뿐이라 라이브러리 도입 없이 충분히 관리 가능하기 때문이다.
인증 토큰은 zustand store에서 관리하며, axios interceptor로 자동 주입한다.

## Note
소셜 로그인은 후속 PR에서 추가 예정이다.
```

### 버그 수정

```markdown
# 제목
fix(blog): 게시글 상세 페이지에서 마크다운 렌더링 깨지는 오류 수정

# 본문
## Why
게시글 본문에 코드 블록이 포함된 경우 마크다운이 올바르게 렌더링되지 않아
사용자에게 raw 텍스트가 그대로 노출된다.

## What
마크다운 파서가 fenced code block의 language identifier를 처리하지 못하는
문제였다. rehype-highlight 플러그인을 추가하여 코드 블록 파싱과
구문 강조를 함께 해결한다.
```